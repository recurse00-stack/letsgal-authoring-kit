# 0.1.0-rc.1（未发布）

新增完整 MIT；建议启用前停用同类 Skill，保留原有文件与特调；补充按工程复用版本证据，减少无必要的重复确认。

工坊入口源码独立维护，加入可点击及可复制的 GitHub 下载地址、统一风险文本和严格构建脚本。补齐源码发布白名单、ZIP 路径保护与实际备份恢复检查。

独立 AI 两轮已完成。当前官方 Stable／Beta SDK 均有缺文件的类型错误，三款模型客户端、原生安装操作与双宿主验收仍有缺口；本候选不能标记为正式通过，详见 VALIDATION.md。

---

# 0.1.0-preview.7

- 统一对外声明：按现状提供、不提供担保，作者及贡献者不对因使用或无法使用本包造成的损失承担任何责任。
- 说明版本更新为 2026-09-26.2，同步安装摘要、随 Skill 全文、手册与工坊指引；保留具体风险和防误删要求。
- 此次只调整提示文字和版本，不改变安装保护流程、不新增版权许可。

# 0.1.0-preview.6

- 加入中文风险说明与免责声明，明确 AI 误改、误删、资料外发、兼容和费用风险。
- 安装界面顶部展示摘要并可展开全文；命令行在修改前输出提醒，非交互运行同样保留。
- 说明随 Skill 安装，手动导入也可阅读；AI 首次准备写工程时核对风险、备份和权限，避免重复泛泛告警。
- 安装收据与操作日志记录说明版本和校验值，不推定用户已阅读、同意免责或放弃权利；不联网收集记录。
- 明确技能目录备份不等于作品备份；此次变更不新增 MIT 或其他版权许可。

# 0.1.0-preview.5

- 安装器状态改为原子替换，避免硬链接把写入传给其他用户文件；新日志使用独占新建。
- 选项／日志保存失败单独显示提示，不再把已完成的安装报告为失败；界面补充旧技能备份位置并自动滚动显示结果。
- 残缺或异常类型的历史选项安全回退，不阻断后续选择。
- auto 检查兼容旧分支省略 mode，显式 fragments 模式保留新建格式要求，原 JSON 不改写。
- 补充手动更新、日志恢复位置与项目内备份的 Git 排除说明。

# 0.1.0-preview.4

- 用户区明确分为 preferences/ 个人偏好与 plugins/ 用户插件 Skill；公共技能更新、卸载都保留。
- 新安装使用 preferences/user.md；旧版根目录 user.md 原位兼容，不自动搬家、不新建空白文件遮住旧配置。
- 分析插件并生成 Skill 时，默认写入 plugins/<插件ID>/<版本>/SKILL.md，按需登记索引；用户已有内容保全后局部维护。
- 项目选择绑定实际插件版本；插件代码和资料不随公共包更新，不自动启用或注册全部 Agent。
- 安装界面分别显示两类用户目录，新增真实旧版升级、原文保留及失败保护检查。

# 0.1.0-preview.3

- 补齐使用手册、功能简介、隐私边界和稳定版／Beta 项目兼容规则。
- 按项目的完整 Studio／SDK 版本判断能力，不默认 Beta，不自动迁移旧工程或启用蓝图。
- 未知章节／索引格式返回 unsupported_format，新增 json-only 语法检查模式。
- 安装器增加跨进程锁，拒绝相对／设备路径，提前检查配置对象类型，修复失败回滚对并发目标的处理。
- 无效 DSH 路径不再阻止其他 Agent 打开图形界面。
- 发行包采用明确文件清单；不收集整个工作区，附带隐私扫描及打包边界回归。
- GitHub 发布准备与工坊审核分别记录；最新验证结果见 VALIDATION.md。

# 0.1.0-preview.2

- 中文图形导入器：选择 Agent、选择范围、预览位置、点击导入，完成后复制验证提示词。
- 新增 DSH 专用适配：DSH_HOME／自选数据目录、项目 Git 根识别，并用官方技能 provider 验证发现与读取。
- 图形入口支持检查、保留特调的卸载、发现本地修改后备份再继续；后端沿用文件校验及完整备份。
- 五种 Agent 与自选目录；保存安装选择。个人 user.md 和项目 LETSGAL.md 继续独立保留。
- 100 项隔离检查通过，包括 46 项基础回归、26 项 DSH 检查、28 项 WPF 组件事件检查；真实 Agent 会话及工坊仍待验收。

# 0.1.0-preview.1

- 从官方教程独立编写 letsgal-authoring，与其他名称的个人技能分开。
- 制作流程、官方资料定位、章节 JSON、扩展开发、协作、Git 与防误删说明。
- 原创双选项章节和只读部分静态检查器。
- Windows 交互式安装／检查／卸载；旧版完整备份，哈希校验及修改冲突检测。
- 个人 user.md 与工程 LETSGAL.md 独立于公共技能，升级无需重配。
- 官方目录规范适配 Codex、Claude Code、Cursor、Copilot；真实客户端加载和 LetsGal 宿主验收仍需完成。

作者署名及公开发行许可证待发布者选择；未附带第三方 SDK、引擎或素材。
