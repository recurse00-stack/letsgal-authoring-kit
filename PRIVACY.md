# 发布与隐私边界

核心发行包只选取 `release-files.json` 明确列出的源码、公共技能、原创教学示例和文档。GitHub 源码另包含 `workshop-source-files.json` 及其中列出的工坊源码，使用 `maintenance/stage_publication.py` 在新目录准备。工坊审核包由 `maintenance/build_workshop.py` 构建并逐项校验。工作目录、测试样本、真实项目、安装日志、个人配置和 Git 元数据不在这些清单中；SDK、node_modules 和构建缓存也不上传。

导入器不包含下载、遥测或账号访问逻辑；本地操作仅用于目标 Skill、独立用户区的缺失目录／首次偏好文件、选择记录和备份。已有偏好、用户插件 Skill 和插件代码不覆盖；用户区不随公共技能更新打包上传。使用者在自己的机器运行后，选择记录与日志会包含实际安装路径，这些文件不应上传到公共仓库。

Skill 会指导 AI 按任务查阅官方网页，具体联网和项目访问由用户正在使用的 Agent 及权限控制。不能将“安装器离线”理解为此后所有 AI 会话都不会联网。

发布检查覆盖完整发行文件文本、常见令牌、私钥、邮箱、本机路径及本机用户名标记；提交前另看准确文件清单与 Git 提交元数据。自动扫描有范围限制，不能代替人工检查；本包不附带来自真实用户会话的截图。

公开 GitHub 仓库会显示其 GitHub 所有者，这与包内不包含本机用户名、私人路径、联系方式、账号凭据和私人作品是不同范围。不要为了发布而把本机 Git 的真实姓名和邮箱自动带入提交。

安装收据与安装／卸载日志还记录风险说明版本、SHA-256 及输出渠道；仅在本地用于文案追溯，不收集同意或放弃权利，不自动上传。人工分享日志前仍应脱敏真实路径。
