# 稳定版、Beta 与项目兼容

版本选择属于当前作品。不要把制作此 Skill 时使用的版本、某个作者的 Beta 环境或“最新文档”当作所有用户的环境。Skill 包自身的 preview 版本号、Agent 版本、Studio 版本和扩展 SDK 版本是四件不同的事。

## 写入前确定目标

读取项目已有的 LETSGAL.md／开发说明，并核对用户指定的 Studio 完整版本与发布通道（stable、beta 或 unknown）。优先用该实例的“关于”信息或安装包版本佐证；工程 JSON 中名为 version／engineVersion 的字段未必表示编辑器版本，不凭名称推断。

记录本次真正需要的内容：Studio 完整版本、通道、事实来源、调度方式、相关扩展及 SDK 版本、已有可工作样本。资料不全时只补问会影响当前写入的那一项；剧情讨论、只读检查、文案草稿等独立工作可以继续。

对每个新字段或 API，按以下顺序判断：目标版本实际保存的样本／SDK 类型 → 该版本官方说明和更新记录 → 当前文档作为检索线索。最新文档描述的存在不等于旧版支持；样本中没出现某字段也不能单独证明不支持。

## 按功能判断，不只按标签

| 目标 | 工作方式 |
| --- | --- |
| 稳定版且功能已确认 | 使用该版已有字段、指令和接口；不加入仅在更晚版本出现的能力 |
| Beta 且功能已确认 | 只启用当前请求用到的能力，按完整 Beta 版本和配套 SDK 记录依据 |
| 旧稳定版／功能不确定 | 沿用已存在的制作方式；新 API 或结构先在独立样例中验证 |
| 版本未知／证据冲突 | 保留原文和结构，继续无版本依赖的工作；暂不写入有争议的新字段 |

官方明确将章节蓝图标为 v2.0.0 新增，因此不能把蓝图工作流直接套到 1.x 工程。2.0.0 之后也要确认当前工程是否启用了它，不能仅凭版本切换调度。[官方章节调度说明](https://docs.avg-engine.com/advanced/chapter-scheduling)

查 [官方更新日志](https://avg-engine.com/changelog) 时保留完整版本后缀和查阅日期；不要按字符串大小比较版本。此文不维护一个猜测的“所有版本字段支持表”，没有依据的最低版本保持未知。只有用户明确选择升级时才安排升级与迁移；不为方便 AI 自动换到 Beta。

## JSON、SDK 与降级

章节的 fragments/blocks 结构和序列化参数仅在目标版本格式已确认时使用。不能把字段名不一致、旧版结构或扩展字段当作垃圾，不能将未知 JSON 自动重写成当前示例。先让目标 Studio 在隔离工程保存一份最小对应指令，再比对字段和运行行为。

检查器默认 auto 只识别已有片段结构；未知章节格式标为 unsupported_format，退出码 2，不自动转换。`--format json-only` 只检查语法。结构检查通过也不证明稳定版／Beta 的功能、SDK 或运行时兼容。

扩展采用目标 Studio 实际提供的 SDK；不得把 Beta 的 sdk/ 拷进稳定版工程，不压低清单要求来掩盖 API 缺失，不以 TypeScript 编译成功代替宿主运行。Beta 工程需要回稳定版时，先保留完整副本，比较受影响字段与接口；没有确认的逆向迁移方式就保留原版。

## 避免跨工程冲突

同一个 Skill 服务不同版本工程，版本约定写在各自项目的 LETSGAL.md；个人 user.md 可写一般偏好，不能覆盖项目明确选定的版本。不要同时安装同名“稳定版 Skill”和“Beta Skill”，也不要设一个全局 Beta 开关影响全部作品。

切换项目时重读该项目的版本约定，必要证据按“项目 + Studio 完整版本 + SDK”区分。旧项目缺少版本约定时按 unknown 处理，不能沿用上个任务的 Beta 结论。只记录有用的兼容事实，不复制整站文档。
