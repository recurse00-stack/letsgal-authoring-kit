# 0.1.0 发行范围与检查入口

2026-09-26 调整：不方便开展的运行实测暂缓，理论兼容可以发行，但必须公开标明未实测。不能把暂缓改写为通过。历史记录保留在 VALIDATION.md 和 validation-results.json。

## 发行仍须完成

1. 保留资料保护、备份恢复和隐私检查；安装器有保护失败就停止发布。
2. Stable 2.0.0、Beta 2.2.0-beta.1 分别使用真实官方 SDK 做严格类型检查和 Vite 构建。SDK 不改写；类型入口的范围及理由见 SDK-BLOCKER.md。声明 ^2.0.0 以共同接口和官方版本校验结果为依据。
3. 固定版本与清单，核对源码映射、外部依赖、风险文本、许可证、内嵌核心 ZIP 和校验值。源码与归档采用明确白名单，不收集 SDK、node_modules、日志、截图、账号资料或私人路径。
4. stage_publication.py 生成全新公开暂存，再审查准确差异；保留历史 preview.7，不覆盖旧 ZIP。
5. 发布 GitHub 正式资产后匿名下载，核对哈希并从下载包完成隔离干净安装。最终字节收据单独附于发行页，避免归档自引用校验值。
6. 用同一工坊目录投稿：开放源码，填写公开仓库和真实下载地址，至少一个合适标签，简介与 README 明示能力和验证范围。检查预检和提交后的真实状态；没有成功回执不能记为已提交。

## 暂缓的实测

三款 Agent 的完整模型任务／更新后新会话、安装器原生点击／取消／选目录／缩放，以及两版 Studio 同包运行、合成剧情、变量、存读档、Windows 导出。无需为本次发行新增个人模型凭据访问。Cursor、Copilot 仅目录适配；Windows 提供安装器，其他系统仅手动说明。

独立 AI 两轮已完成，不另启第三轮。审核通过后的真实工坊下载、内嵌 ZIP 提取和导入另行验收。投稿、通过审核和用户下载安装分别记录，不相互替代。
