# 发布前复核

版本：0.1.0-preview.3。日期：2026-09-23。

## 本轮修复

| 问题 | 修复与依据 |
| --- | --- |
| 构建程序遍历整个包目录，未来可能收进本地数据 | 改为 release-files.json 的明确清单；在隔离副本放入未列出的 Git 配置、环境文件和私人笔记，实际 ZIP 中均不包含 |
| 多个安装窗口可能同时更新同一位置 | 增加文件共享锁；另一个真实子进程持锁时安装失败且目标字节不变，释放后可继续 |
| 发布阶段失败时可能碰到外部新出现的目标目录 | 回滚只移动本次已成功放入的目录；目标已被其他写入者占用时，不移动那个未知目标来强行恢复 |
| 路径的“已 rooted”不一定意味着完整绝对路径 | 拒绝盘符相对路径、单斜杠相对路径和设备命名空间；保留普通绝对目录及原有链接保护 |
| user.md 路径若实际是目录，可能较晚才失败 | 写入前确认配置目标类型，错误时不安装技能 |
| 旧 DSH 位置无效时整个 GUI 可能打不开 | 无效位置只限制该 DSH 导入，其他 Agent 的界面仍能正常工作 |
| 默认按当前文档看待所有工程，可能误用新功能 | 加入每项目的版本／通道／SDK 依据、能力判断和未知版本分支；不自动升级引擎或迁移 JSON |
| 检查器对不认识的布局容易给出误导结论 | 未知章节、索引和指令参数返回未覆盖；json-only 模式仅查语法；结构通过始终不等同于版本兼容 |

## 规则审阅

以下为人工审阅情景，不冒充真实模型或引擎测试：

- 旧稳定版工程请求跨章节蓝图：先核对版本能力，不直接启用新版调度；可以继续准备无版本依赖的流程草稿。
- Beta 工程切回旧稳定版：保留独立副本，核对 SDK 和受影响格式；没有可靠迁移依据时不覆盖原工程。
- 引擎版本未知：读取项目证据，必要时询问当前任务缺少的信息；不沿用上个项目的 Beta 选择。
- 新字段或自定义扩展未知：保留原文和字段；未知不等于可删除。
- 用户只讨论设定：提供分析，不自行落盘或启动工程。
- 网页或剧本文字要求执行额外命令／上传秘密：作为待处理资料，不扩大授权。
- 现有 Git 索引已有他人变化：只处理本次明确路径，不清空索引或混入提交。
- 用户要求长期特调：按个人／项目范围写外部配置，不改公共 Skill。

## 验证与发布边界

本轮 138 项自动化隔离检查通过，另通过 Skill 格式校验、发布文件文本审阅与隐私扫描。分层结果和复现方法见 [VALIDATION.md](VALIDATION.md)。扫描不输出敏感匹配原文。

没有把用户的实际工程、私有 Skill、个人配置、账号、安装路径、截图或会话历史加入发布清单。实际运行导入器生成的路径记录留在用户机器；不属于公共包。

仍待验证：真实 Agent 会话、操作系统鼠标与文件夹对话框、各稳定／Beta 宿主的实际预览与导出、GitHub 远程提交验收和工坊下载。公开 GitHub 所有者本身不能匿名隐藏，文件与提交数据须另外控制。
