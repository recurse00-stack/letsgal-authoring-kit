# 0.1.0-preview.6 风险告知复核

新增集中风险说明、依法限制的免责声明、安装前可见的摘要与全文、CLI 提示及本地文案版本记录。说明包含 AI 误改误删、资料外发、版本兼容和费用风险，并区分技能备份与作品备份。手动复制后的 Skill 也保留全文。

AI 首次写入工程时按实际范围告知并核对备份，不以安装或泛泛“接受风险”扩大删除、上传或付费权限，不重复宣读未变化的提示。安装／更新的原有文件保护流程保持，新增记录不收集同意、不上传遥测。

本次 156 项检查通过；说明缺失时安装在用户目录写入前停止。风险说明不能保证零损失或全部免责，没有新增 MIT 或其他版权授权。版本、文案和校验值随新发行物更新，旧版本档案保留。

---

# 0.1.0-preview.5 全面复核

日期：2026-09-25。

本轮逐项阅读安装后端、界面、路径处理、检查器、构建与隐私工具，以及使用／版本／插件规则；复核官方 JSON、扩展开发和章节调度入口。

| 发现 | 修复 |
| --- | --- |
| 状态文件若为硬链接，原先截断写入会更改另一文件；已用隔离文件复现 | 同目录临时文件校验写完后原子替换目录项，关联文件原字节保留；新日志独占创建 |
| 选项写入失败发生在 Skill 就位之后，原先会误报整个安装失败 | 核心文件结果与附属记录警告分别返回，界面展示提示与备份位置，并自动滚动到完成结果 |
| 有效 JSON 但缺少字段或字段类型异常的历史选项可能影响启动 | 共享读取器逐项校验、补齐安全默认，不改写失败原文 |
| 旧分支省略 mode 被检查器判错，但官方明确兼容为 jump | auto 按兼容默认检查并警告，严格 fragments 模式仍要求新建字段完整；坏引用仍报错 |
| 手动更新、日志定位、项目内备份提交边界不够清楚 | 补充完整旧目录保全、不合并覆盖、恢复记录位置与 Git 排除说明 |

测试数量及当前结果见 [VALIDATION.md](VALIDATION.md)。没有改动真实用户区、插件工程或私有 Skill。下文为历史复核，旧版待办状态不代表当前远端状态。

---

# 0.1.0-preview.4 复核

日期：2026-09-25。

- 个人 preferences/ 与插件 plugins/ 独立；界面、主 Skill、使用手册与自动落位模板使用同一约定。
- 新安装创建 preferences/user.md；旧版 user.md 原位沿用，不创建空白文件遮挡它；两者并存不自动合并或删除。
- 用户区初始化只创建缺失项，以 CreateNew 写首次偏好，不截断已有文件；初始化失败发生在替换旧 Skill 之前。公共 Skill 本地改动仍默认阻止更新，明确备份后才替换。
- 真正使用未修改的 preview.3 安装包建立基线，再升级到 preview.4；核对旧技能完整备份以及全部用户文件原始字节。
- 插件 Skill 按插件 ID／版本存放，插件索引不等于启用清单，实际项目选择写 LETSGAL.md；没有自动升级插件或修改其代码的逻辑。

人工规则复核情景（并非真实模型运行）：生成新插件 Skill 默认进入用户区；同路径已有用户资料时保全并局部维护；旧项目不会取最新插件资料；未知版本保持未确认；插件文档中的命令不能扩大权限；生成插件 Skill 不自动修改依赖或注册全部 Agent。

172 项隔离检查通过，另通过 Skill 格式校验。插件 Skill 自动落位是给 AI 的工作规则；不同 Agent 的真实模型调用仍未验收。详细范围与复现见 [VALIDATION.md](VALIDATION.md)。发布包只包含公共模板和规范，不包含用户实际插件或偏好。

以下为 preview.3 的历史复核记录，保留作为已完成基础修复的依据；其当时的待发布状态不代表当前远端状态。

---

# 发布前复核

版本：0.1.0-preview.3。日期：2026-09-23。

## 本轮修复

| 问题 | 修复与依据 |
| --- | --- |
| 构建程序遍历整个包目录，未来可能收进本地数据 | 改为 release-files.json 的明确清单；在隔离副本放入未列出的 Git 配置、环境文件和私人笔记，实际 ZIP 中均不包含 |
| 多个安装窗口可能同时更新同一位置 | 增加文件共享锁；另一个真实子进程持锁时安装失败且目标字节不变，释放后可继续 |
| 发布阶段失败时可能碰到外部新出现的目标目录 | 回滚只移动本次已成功放入的目录；目标已被其他写入者占用时，不移动那个未知目标来强行恢复 |
| 路径的“已 rooted”不一定意味着完整绝对路径 | 拒绝盘符相对路径、单斜杠相对路径和设备命名空间；保留普通绝对目录及原有链接保护 |
| user.md 路径若实际是目录，可能较晚才失败 | 写入前确认配置目标类型，错误时不安装技能 |
| 旧 DSH 位置无效时整个 GUI 可能打不开 | 无效位置只限制该 DSH 导入，其他 Agent 的界面仍能正常工作 |
| 默认按当前文档看待所有工程，可能误用新功能 | 加入每项目的版本／通道／SDK 依据、能力判断和未知版本分支；不自动升级引擎或迁移 JSON |
| 检查器对不认识的布局容易给出误导结论 | 未知章节、索引和指令参数返回未覆盖；json-only 模式仅查语法；结构通过始终不等同于版本兼容 |

## 规则审阅

以下为人工审阅情景，不冒充真实模型或引擎测试：

- 旧稳定版工程请求跨章节蓝图：先核对版本能力，不直接启用新版调度；可以继续准备无版本依赖的流程草稿。
- Beta 工程切回旧稳定版：保留独立副本，核对 SDK 和受影响格式；没有可靠迁移依据时不覆盖原工程。
- 引擎版本未知：读取项目证据，必要时询问当前任务缺少的信息；不沿用上个项目的 Beta 选择。
- 新字段或自定义扩展未知：保留原文和字段；未知不等于可删除。
- 用户只讨论设定：提供分析，不自行落盘或启动工程。
- 网页或剧本文字要求执行额外命令／上传秘密：作为待处理资料，不扩大授权。
- 现有 Git 索引已有他人变化：只处理本次明确路径，不清空索引或混入提交。
- 用户要求长期特调：按个人／项目范围写外部配置，不改公共 Skill。

## 验证与发布边界

本轮 138 项自动化隔离检查通过，另通过 Skill 格式校验、发布文件文本审阅与隐私扫描。分层结果和复现方法见 [VALIDATION.md](VALIDATION.md)。扫描不输出敏感匹配原文。

没有把用户的实际工程、私有 Skill、个人配置、账号、安装路径、截图或会话历史加入发布清单。实际运行导入器生成的路径记录留在用户机器；不属于公共包。

仍待验证：真实 Agent 会话、操作系统鼠标与文件夹对话框、各稳定／Beta 宿主的实际预览与导出、GitHub 远程提交验收和工坊下载。公开 GitHub 所有者本身不能匿名隐藏，文件与提交数据须另外控制。
